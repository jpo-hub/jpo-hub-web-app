# Variables de la stack docker-compose — copier vers .env et renseigner
POSTGRES_USER=jpo-hub
POSTGRES_PASSWORD=changez-moi
POSTGRES_DB=jpo-hub-db
# Secret de signature des JWT admin (générer : openssl rand -base64 32)
JWT_SECRET=changez-moi-aussi
# Port HTTP public du reverse proxy
HTTP_PORT=80
